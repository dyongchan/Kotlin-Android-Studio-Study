package fastcampus.part0.kotlin

fun main() {
    // 변수
    // val = value (값)
    // var = variable (변경 가능한)

    val a: Int = 3
    var b: Int = 10 // 변수 타입을 뒤에씀

    b = 20

    // a = 20 값이 지정 된 후에 재할당 불가 (value)

    var name = "jyc" // 변수타입을 써주지 않아도 타입을 출현해준다.
}